<% content_for(:title) { "Edit #{@team.name} — Capacity Planning" } %>

<div class="planning-layout">
  <header class="planning-header">
    <div>
      <p class="planning-header__eyebrow">Management</p>
      <h1>Edit Team</h1>
      <p>Update details for <%= @team.name %>.</p>
    </div>
    <%= link_to "Back to Teams", planning_teams_path, class: "planning-btn" %>
  </header>

  <section class="planning-form-card">
    <%= render "planning/teams/form",
              team: @team,
              url: planning_team_path(@team),
              method: :patch,
              submit_label: "Update Team" %>
  </section>
</div>
