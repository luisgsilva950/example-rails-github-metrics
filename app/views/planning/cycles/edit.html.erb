<% content_for(:title) { "Edit #{@cycle.name} — Capacity Planning" } %>

<div class="planning-layout">
  <header class="planning-header">
    <div>
      <p class="planning-header__eyebrow">Sprints</p>
      <h1>Edit Cycle</h1>
      <p>Update details for <%= @cycle.name %>.</p>
    </div>
    <%= link_to "Back to Cycles", planning_cycles_path, class: "planning-btn" %>
  </header>

  <section class="planning-form-card">
    <%= render "planning/cycles/form",
              cycle: @cycle,
              url: planning_cycle_path(@cycle),
              method: :patch,
              submit_label: "Update Cycle" %>
  </section>
</div>
